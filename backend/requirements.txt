# IsikSchedule backend top-level dependencies (manifest, not freeze).
# pip resolves transitive deps. For reproducible installs, generate a
# separate requirements-lock.txt with `pip freeze`.

# --- Web framework / server ---
fastapi==0.136.0
uvicorn[standard]==0.44.0
python-multipart==0.0.31

# --- Data validation / settings ---
pydantic==2.13.2
pydantic-settings==2.13.1
email-validator==2.3.0
python-dotenv==1.2.2

# --- Persistence ---
SQLAlchemy==2.0.49

# --- Auth ---
python-jose[cryptography]==3.5.0
passlib==1.7.4
# bcrypt pinned to 4.0.x: passlib 1.7.x is incompatible with bcrypt>=4.1.
bcrypt==4.0.1

# --- Excel / data ---
pandas==3.0.2
openpyxl==3.1.5

# --- Dev / test ---
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
httpx==0.28.1
ruff==0.15.11
